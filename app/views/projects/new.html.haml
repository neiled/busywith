= content_for :unobtrusive_javascript do
  %script{ 'type' => "text/javascript", :src   => "/javascripts/getsatisfaction.js" }
  
Add Project

- form_for [@team, @project] do |f|
  = f.error_messages
  = f.label :name, "Project Name"
  = f.text_field :name
  = f.submit "Add Project", :disable_with => 'Adding Project...'
